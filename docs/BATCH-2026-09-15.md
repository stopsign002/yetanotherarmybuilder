# Batch 2026-09-15 — issue-solver run

Unattended run `run-20260915-0600`. Two workers, distinct files. The app is
served straight from this working tree, so **every saved edit is live**:
keep edits syntactically valid at every save (`node --check <file>` after
each). Workers do not commit, do not touch `js/data/changelog-data.js`,
`index.html` or `sw.js` — the session adds the changelog entries and runs
`scripts/stamp-assets.mjs` once at the end.

Shared cheap check for both workers: after editing, open
`https://yaab.thewheeliebois.com/` with
`~/sites/base/browser/browse.sh shot <url> <own outdir>` and confirm no
console errors were introduced (compare against a shot taken BEFORE your
first edit — take that first).

Repo idiom: IIFEs, no imports, comments explain *why*. Match it.

---

## Worker A — UI/logic fixes: #71, #82, #87 (then #73 if time allows)

Owned files: `js/app/events.js`, `js/app/wargear-picker.js`,
`js/ui/cards-mode.js` (the ×N chip lookup only, ~line 1114),
`js/app/attachments.js`, `js/app/settings-drawer.js` (only if doing #73),
`js/app/mobile-history.js` (only if doing #73). Nothing else.

### #71 — removing an army entry leaves `selectedArmyEntryIndex` stale

`js/app/events.js` ~196-206: after `removeEntry(index)`:
- if `selectedArmyEntryIndex === index` → null + `UI.clearUnitDetail()` (as now);
- **else if `selectedArmyEntryIndex > index` → decrement by one.**
Check `js/app/wargear-picker.js` ~32-49: a module-local `entryIndex` is
only set in `mount()`. Make the removal path reconcile it — simplest is
to have the picker read the index from `App.state.selectedArmyEntryIndex`
at write time, or re-mount. Also grep for any *other* splice sites
(`js/app/split-entry.js`, `js/ui/flip-animations.js` reorder) — if they
already handle the shift, leave them; if they call a shared helper, use it.

Verify with a `browse.sh run` script: build an army of three units (pick
any faction; add via the roster's Add button), click the third entry so it
is selected, remove the first, assert the detail pane still shows the
originally selected unit's name and `App.state.selectedArmyEntryIndex ===
1`. Read the PNG.

### #82 — cards-mode ×N chip misses plural item names

`js/ui/cards-mode.js` ~1114: after the base-name fallback, add a
trailing-`s`/`z` tolerant fallback in BOTH directions (row "Choppa" vs item
"Choppas"; also try stripping a trailing `s` from the row name, and
appending one). Keep it to those two forms — no fuzzy matching.
Verify: enable Cards → "Wargear counts (×N taken)", add Ork Kommandos,
open Cards mode, assert the Choppa row carries a `.dcc-wg-count` chip.
Shot + read the PNG.

### #87 — prose parser needs a colon; injected Leader/Support ability becomes a phantom target

`js/app/attachments.js` ~70-72 and ~161-162 (same three lines twice).
Fix at the parser: when the `replace()` finds no colon (i.e. the string is
unchanged after the replace), **skip that ability** — it is a sentence, not
a list. Factor the three lines into one helper used by both call sites so
they cannot drift. Do not touch `ATTACH_ROLE_ABILITY` in dc-adapter.
Verify with a `browse.sh run` script: for every unit in `App.state.allUnits`
with a non-null `attachmentRole`, assert no entry of
`App.Attachments.candidateTargetsFor(u)` contains "can be attached to"
(case-insensitive). Report the count of units checked. Then confirm a
known-good prose case still works: pick one non-Ork leader whose 40kdc
ability text has a real "can be attached to … :" list (find one by
scanning `allUnits` in the same script) and assert its targets are
non-empty.

### #73 (optional, only after the three above verify)

`js/app/settings-drawer.js` `goToMode()`: `close()` triggers
`App.backTrap.closed('drawer')` → async `history.go(-1)`, then
`App.setMode()` → `pushState` lands before the go(-1) is processed. Fix:
defer the mode switch until the popstate from the close has been consumed —
e.g. if `App.backTrap.isTrapped('drawer')` (check the actual id the drawer
uses) then listen once for `popstate` (or use a `setTimeout(…, 0)` only if
you can show it is reliable in headless Chrome at 390 px) and call
`App.setMode(mode)` there. Verify at 390×844 with `browse.sh run`: open
the drawer, click the Collect row, `page.goBack()`, assert the URL is still
yaab and `App.getMode() === 'build'`. If it does not verify in two tries,
leave the file untouched and say so.

---

## Worker B — data plumbing: #79, #80

Owned files: `js/gdc.js`, `js/data/dc-adapter.js`,
`/home/stopsign002/sites/base/mfm-aliases.json` (with a `.bak-20260915`
copy first, and the mirror at
`/home/stopsign002/server-cron/scripts/base/mfm-aliases.json` if one exists
— check; do not commit in server-cron, the session does). Nothing else.

### #79 — detachment rule headed with the detachment's name

`js/gdc.js` ~480-502: the fill-only path only replaces `d.rules` when no
rule has a description. Add a **heading-only** pass: when 40kdc supplied
text (so we keep it) and GDC's `built` for that detachment has exactly one
rule, or a rule whose folded name differs from the detachment's folded
name, set `d.rules[i].name` from GDC's rule name **when our current name
equals the detachment name** (i.e. it is the placeholder `toDetachment`
wrote). Never change description on this path. Keep `source` as it was.
Also in `js/data/dc-adapter.js` `toDetachment()` (~1869): if
`d.detachment_rule_ids` is an array and `detachment_rule_id` is null/empty,
use its first entry with text. Comment why.

Verify with `browse.sh run`: select Space Marines (not Orks — Orks are on
the authoritative path already), read
`App.state.factions` (or whatever holds the current faction) for the
Gladius Task Force detachment and assert `rules[0].name` is GW's rule name
("Combat Doctrines"), not "Gladius Task Force". Then shot the Build page
with the detachment rule panel open and read the PNG. Also assert at least
90% of all detachments across all factions now have `rules[0].name !==
detachment.name` and print the remainder.

### #80 — `abilities.wargear` never read; dead adopt rows

1. `js/gdc.js` `project11Abilities` (~718) reads only `abilities.other`.
   For the non-authoritative fill path add `abilities.wargear` entries in
   the same shape, **but** route them so they fill `unit.wargearAbilities`
   (the shape the authoritative path in dc-adapter uses for GDC
   `abilities.wargear` — read that code and mirror the shape exactly),
   fill-only (never overwrite existing text). Do not add them to the
   ordinary `abilities` array.
2. `js/data/dc-adapter.js` `ADOPT_UNITS` (~2842): remove the `'orks'` row
   (keep the map and the mechanism, with a comment that it is empty
   because 40kdc 1.4.2 ships those units). First **prove** they are dead:
   `grep -c '"id":"gunwagon"' js/vendor/dc-bundle.js` etc. for all four.
3. `mfm-aliases.json`: remove the four `unit_adopt` rows (GUNWAGON,
   NAZDREG, RUNTHERD, WARBUGGIES) — after confirming
   `~/sites/base/mfm-scrape-wargear.py` only uses `unit_adopt` to feed the
   adopt path and that the units now match by id directly (grep the
   scraper; if `unit_adopt` also drives matching, leave the rows and say so).

Verify: `browse.sh run` — assert the Ork roster still contains Gunwagon,
Nazdreg, Runtherd and Warbuggies (by name) and that a non-Ork unit whose
GDC file carries `abilities.wargear` but whose 40kdc data lacks it now
shows it (find one by scanning `data/gdc/11th/gdc/*.json` vs the live
`allUnits`; if none exists, say so — the change is then verified as a
no-op with no regression). Shot the Orks Build page and read the PNG.

Also run `node scripts/parser-coverage.mjs` before and after and confirm
the faction/unit counts did not move.
